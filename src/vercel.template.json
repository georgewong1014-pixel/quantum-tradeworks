{
  "$comment": [
    "GENERATED BY build.mjs — edit src/vercel.template.json, not vercel.json.",
    "The script-src hash is computed from the assembled inline <script>, so it",
    "changes whenever the app changes. `node build.mjs --check` fails if the",
    "committed vercel.json no longer matches the committed index.html, which is",
    "the failure that would otherwise take the whole site down in production",
    "while every local check still passed."
  ],
  "$comment2": [
    "WHY THESE THREE FIELDS EXIST.",
    "Adding a \"build\" script to package.json changed how this project deploys.",
    "Vercel auto-detects one, runs it, and then looks for a build output",
    "directory — which this project does not have and never will, because the",
    "artefact is index.html at the repo root and it is committed. Four",
    "consecutive deployments failed that way while every local check and all of",
    "CI stayed green.",
    "",
    "So the behaviour is stated rather than inferred. installCommand is empty",
    "because build.mjs imports nothing outside node builtins, and skipping it",
    "avoids downloading sharp's binaries on every deploy for no reason.",
    "buildCommand verifies rather than builds: if index.html has drifted from",
    "the src/ it claims to come from, the DEPLOY fails, not just CI.",
    "outputDirectory is the repo root — exactly what was served before any of",
    "this, so nothing new becomes reachable."
  ],
  "installCommand": "",
  "buildCommand": "node build.mjs --check",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/((?!_vercel/|data/|.*\\..*).*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "$comment": [
        "Versioned data. dataUrl() appends ?v=<content hash> from DATA_VERSIONS,",
        "which build.mjs stamps into the app, so a URL's contents can never",
        "change — a year of immutable caching is safe and a changed file simply",
        "arrives under a new URL. us.json alone is 1.4MB; before this it was",
        "re-fetched on every navigation because fetchJson sent no-store.",
        "",
        "An UNVERSIONED /data request (a licensed-lane file that only exists on",
        "the reader's own machine) still gets no-store from fetchJson itself.",
        "max-age here is the CDN's and the browser's ceiling, not a floor."
      ],
      "source": "/data/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    },
    {
      "$comment": [
        "The app shell carries its own version, so it must always be revalidated.",
        "no-store rather than must-revalidate: a stale shell paired with a new",
        "data manifest is the one combination that renders wrong numbers, and",
        "the file is a single 1.3MB gzip-compressible response on a CDN edge."
      ],
      "source": "/((?!data/).*)",
      "headers": [
        { "key": "Cache-Control", "value": "no-cache" },
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'self'; script-src 'self' '@CSP_HASH'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; form-action 'none'; base-uri 'none'; frame-ancestors 'none'; object-src 'none'"
        },
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        { "key": "Permissions-Policy", "value": "geolocation=(), camera=(), microphone=(), payment=(), usb=(), interest-cohort=()" },
        { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" },
        { "key": "X-Frame-Options", "value": "DENY" }
      ]
    }
  ]
}
